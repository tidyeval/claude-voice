[project]
name = "claude-tts-server"
version = "0.1.0"
description = "Local Kokoro-82M TTS server for Claude Code post-hook"
requires-python = ">=3.10,<3.13"
dependencies = [
    "kokoro>=0.9.4",
    "soundfile",
    "flask",
]

[dependency-groups]
dev = [
    "pytest>=8.0.0",
    "pytest-cov>=5.0.0",
]
